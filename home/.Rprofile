options(
  repos = c(CRAN = "https://cloud.r-project.org"),
  stringsAsFactors = FALSE,
  useFancyQuotes = FALSE
)
if (interactive()) {
  suppressMessages(require(devtools, quietly = TRUE, warn.conflicts = FALSE))
  suppressMessages(require(lintr, quietly = TRUE, warn.conflicts = FALSE))
  suppressMessages(require(styler, quietly = TRUE, warn.conflicts = FALSE))
}